<%= form_with(model: instrument, local: true) do |form| %>
  <% if instrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instrument.errors.count, "error") %> prohibited this instrument from being saved:</h2>

      <ul>
      <% instrument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label 'Название' %>
    <%= form.text_field :name, id: :instrument_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label 'Тип' %>
    <%= form.text_field :type_name, id: :instrument_type, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label 'Цена' %>
    <%= form.text_field :price, id: :instrument_price, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit 'Добавить', class: 'btn btn-outline-dark' %>
  </div>
<% end %>
